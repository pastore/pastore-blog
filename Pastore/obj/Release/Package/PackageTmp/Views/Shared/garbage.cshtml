        //$('.jqmWindow').jqm({
        //    onHide: loginHide
        //}).jqmShow().animate({
        //    width: width,
        //    top: top + '%',
        //    left: '50%',
        //    marginLeft: '-' + width / 2
        //}, 300);
        //});
        //var loginHide = function (hash) {
        //    var modalWindow = $(hash.w);
        //    modalWindow.animate({
        //        top: '25%',
        //    }, {
        //        duration: 200,
        //        complete: function () {
        //            hash.w.fadeOut(100);
        //            //hash.w.remove();
        //            hash.o.fadeOut(200, function () {
        //                hash.o.remove();
        //            });

        //        }
        //    });
        //};

        var width = 600;
        var height = 300;
        var top = Math.floor(parseInt($(window).height() - height) / 2) * 100 / parseInt($(window).height());
        var top2 = Math.floor(parseInt($(window).height() - height) / 2);
        var left = Math.floor(parseInt($(window).width() - width) / 2) * 100 / parseInt($(window).width());